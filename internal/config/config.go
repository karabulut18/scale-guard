// Package config loads service configuration from environment variables.
// All tuneable parameters (DB DSN, flush interval, config refresh interval) live here.
//
// This package is Phase 2 — implemented alongside the store package.
package config
